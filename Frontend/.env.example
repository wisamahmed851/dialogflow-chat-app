# URL of your NestJS WebSocket gateway (no trailing slash).
# Use a port other than 3000 — that's where the CRA dev server runs.
REACT_APP_SOCKET_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000